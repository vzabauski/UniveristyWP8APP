<Page
    x:Class="App1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel Margin="0,0.333,0,-0.333" Orientation="Vertical">
        <Grid Height="640">
            <TextBlock Text="Users" Style="{ThemeResource HeaderTextBlockStyle}" Margin="0,0,0,567" />
            <ListView x:Name="userList" IsItemClickEnabled="True"
                ItemClick="ItemView_ItemClick" Margin="0,115,0,525">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"  FontSize="26" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:Name="createButton" Content="Add" Click="createButton_Click" Margin="10,200,0,383" />
        </Grid>
    </StackPanel>
</Page>